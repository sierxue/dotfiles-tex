# Aliases related to git.

alias a='git gui' # This alias has been defined in zsh git plugin.
alias amsp="git submodule update --remote --recursive && git add --all && git commit -m 'Update submodules' && git push"
